{
  "name": "e2ejs-example-step-custom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./src/",
    "build:watch": "tsc -p ./src/ -w",
    "clean": "",
    "clean-build": "npm run clean && npm run build",
    "test": "protractor dist/config.js",
    "webdriver-update": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^8.9.5",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "e2ejs": "^1.0.3"
  }
}
